/**
 * Feature flags. Flip to show/hide whole areas of the app without deleting code.
 */
export const FEATURES = {
  /** The interactive 3D house page + dashboard teaser. Hidden for now. */
  house3d: false,
} as const;
